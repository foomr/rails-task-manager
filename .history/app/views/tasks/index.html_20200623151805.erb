<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <div>
    <li>Title: <%= link_to task.title, task_path(task.id) %> Completed? <%= task.completed %></li>
    </div>
  <% end %>
</ul>

<%= link_to "Add a new task", new_path %>